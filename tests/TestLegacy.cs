using System;using System.IO;using HdrPilot;
class TestLegacy{static void Main(string[] a){string root=Path.GetFullPath(a[0]);Directory.CreateDirectory(root);Core.Data=Path.Combine(root,"data");var g=new Game{Name="Fixture",Exe=Path.Combine(root,"Fixture.exe"),Root=root};File.WriteAllText(Path.Combine(root,"hdlss-peak-limit.json"),"{}");string preset="Techniques=Other@Other.fx,HDLSS_PeakLimit@HDLSS_PeakLimit.fx\nTechniqueSorting=HDLSS_PeakLimit,Other@Other.fx\n[Other.fx]\nValue=42\n";File.WriteAllText(Path.Combine(root,"HDLSS-HDR.ini"),preset);string tx=LegacyHdr.Disable(g);string after=File.ReadAllText(Path.Combine(root,"HDLSS-HDR.ini"));if(after.Contains("HDLSS_PeakLimit")||!after.Contains("Other@Other.fx")||!after.Contains("Value=42"))throw new Exception("preserve other effect");if(LegacyHdr.Disable(g)!=null)throw new Exception("not idempotent");DlssCore.Apply(g,DlssCore.Recovery(g,Path.Combine(tx,"transaction.json")));if(File.ReadAllText(Path.Combine(root,"HDLSS-HDR.ini"))!=preset)throw new Exception("restore");Console.WriteLine("PASS: retired technique removed, unrelated effects/settings preserved, repeated operation does nothing, exact preset recovery.");}}
